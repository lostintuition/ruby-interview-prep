require 'spec_helper'

describe Ruby::Interview::Prep do
  it 'has a version number' do
    expect(Ruby::Interview::Prep::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
