require 'spec_helper'

describe RubyInterviewPrep do
  it 'has a version number' do
    expect(RubyInterviewPrep::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
